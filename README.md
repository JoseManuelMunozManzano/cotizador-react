# Ejemplo de Cotizador de Seguros de Vehículos hecho en React

Ejemplo básico que usa Styled Components.

Para instalar ejecutar el comando:

```
npm install
```

Para ejecutar la app ejecutar el comando:

```
npm start
```
